<!--principal: pantalla de bienvenida simple-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp"
    android:background="#F8FCFF"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Texto  bienvenida -->
    <TextView
        android:id="@+id/bienvenidaTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="¡Bienvenid@!"
        android:textSize="24sp"
        android:textColor="#4F3BB8"
        android:textStyle="bold"
        android:layout_marginBottom="24dp"/>

    <Button
        android:id="@+id/irAClasesButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Gestionar Clases"
        android:backgroundTint="#039BE5"
        android:textColor="@android:color/white"
        android:textAllCaps="false"
        android:elevation="2dp"
        android:layout_marginBottom="10dp"/>
    
    <!-- Botón para crear clase de demostración (testing) -->
    <Button
        android:id="@+id/crearClasePruebaButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🎓 Crear Clase de Prueba (Demo)"
        android:backgroundTint="#8E24AA"
        android:textColor="@android:color/white"
        android:textAllCaps="false"
        android:elevation="2dp"
        android:layout_marginBottom="10dp"/>

    <Button
        android:id="@+id/logoutButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Cerrar sesión"
        android:backgroundTint="#E53935"
        android:textColor="@android:color/white"
        android:textAllCaps="false"
        android:elevation="2dp"/>

</LinearLayout>
